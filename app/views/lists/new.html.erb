<div class="banner">

  <div
    class="banner-bg banner-bg-5"
    style="background-image:
      linear-gradient(rgba(0,0,0), rgba(0,0,0)),
      url('<%= asset_path("americanhistoryx.webp") %>');">
  </div>

  <div class="container">
    <h1>Make your own movie list!</h1>
  </div>

</div>

<div class="container my-5">
  <h1>Create a new movie list</h1>
  <!--
  <form action="<%#= restaurants_path %>" method="post">
  </form>
  -->
  <%#= form_with model: @restaurant %>
  <%# Simple: adds the action, method, authenticity token %>
  <%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.submit "Submit", class: 'btn btn-primary mt-2' %>
  <% end %>
</div>
