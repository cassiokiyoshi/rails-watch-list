<%= render "components/banner" %>
<div class="container">
<h2>My lists</h2>
  <div class="cards">
    <%@lists.each do |list|%>
      <% movie = list.movies.sample %>
        <%= link_to list_path(list), class: "card-link" do %>
          <div
              class="card-category"
              style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)),
              url('<%= movie.poster_url if movie %>')">
            <%= list.name %>
          </div>
        <% end %>
  <% end %>
  </div>
</div>
